node_modules
dist
.env
.env.local

# server-side ignores (also covered by server/.gitignore, listed here too
# in case someone runs git from the repo root)
server/node_modules
server/.env
server/data/*.json
!server/data/.gitkeep
